---
"@foresightjs/astro": minor
---

Add `@foresightjs/astro`, an Astro integration that registers links with ForesightJS and prefetches through `astro:prefetch`. Configured like Astro's built-in prefetch, with `"foresight"` as an extra strategy next to the four native ones.
